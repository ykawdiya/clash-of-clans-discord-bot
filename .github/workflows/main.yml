name: Deploy Bot

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'
      - run: npm ci
      - run: npm test

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Railway CLI
        run: npm i -g @railway/cli

      - name: List available services
        run: railway list
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      # After seeing the output of the previous step, uncomment and update this
      # - name: Deploy to Railway
      #   run: railway up --service YOUR_ACTUAL_SERVICE_NAME
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}